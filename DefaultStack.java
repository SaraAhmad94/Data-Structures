package Interview;

import java.util.Stack;

public class DefaultStack {
	public static void main(String[] args)
	{
        // Create a new, empty stack
        Stack lifo = new Stack();
        Stack<Character> myStack = new Stack<Character>();

        char letter = 'a';
        myStack.push((Character) letter);
        System.out.println((Character)myStack.peek());

        // Let's add some items to it
       /* for (int i = 1; i <= 10; i++)
        {
                lifo.push (i);
        }*/
     //   ;
       //   int k =(int) lifo.pop();
       //   System.out.println(lifo.pop());
        // Last in first out means reverse order
      // while ( !lifo.empty() )
       {
              // System.out.print ( lifo.pop() );
            //    System.out.print ( ',' );
    // }//
    	   
        // Empty, let's lift off!
       // System.out.println (" LIFT-OFF!");
	}
}}
